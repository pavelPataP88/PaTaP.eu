# AI_TASK — ROAD_REPORTS_FIX_01

Исправить только замечания Codex по уже готовому ROAD_REPORTS_MVP.

Основа для исправления:
- исходная ветка: `chatgpt/road-reports-mvp`
- кодовый commit: `f11ef2c18be892edddc220bbd908b4d0cc2b6c96`
- полный текст замечаний: верхняя запись `ROAD_REPORTS_MVP / CHANGES_REQUIRED` в `AI_HANDOFF.md`.

Нужно сделать в отдельной ветке от актуального `codex/local-workspace-snapshot`:

1. Гостевой Driver должен видеть только безопасный read-only список дорожных отметок на карте:
   - без authorId, userId, nickname, GPS автора, голосов по пользователям или возможности создать/подтвердить;
   - гость ничего не создаёт и не подтверждает;
   - не сломать lazy MapLibre у гостя: карта по-прежнему не загружается сама по себе без явного открытия.

2. Для подтверждения ЧУЖОЙ отметки `ACTIVE` или `GONE` требовать:
   - действующую сессию и Driver-профиль;
   - свежую добровольно включённую GPS-позицию из текущего механизма;
   - расстояние до точки отчёта не больше `MAX_REPORT_DISTANCE_KM`.
   Автор может закрыть собственную ошибочно созданную отметку без GPS.

3. Добавить реальные интеграционные тесты:
   - гость получает только безопасный список без автора;
   - profile-only/без GPS не может подтвердить чужой отчёт;
   - удалённый водитель не может подтвердить чужой отчёт;
   - близкий водитель может подтвердить;
   - существующие create/TTL/CSRF/privacy тесты сохранены.

Не менять: SQLite, парковки, чат, рацию, Caddy, minimum password 6, main, runtime-данные. Не добавлять свободный текст, фото, speed-trap или обход контроля.

В `AI_HANDOFF.md` сверху укажи branch, точный commit, список файлов, что реально запускалось и что не запускалось. После этого остановись: Codex повторно проверит и только затем переносит локально.
