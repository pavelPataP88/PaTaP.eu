# AI_TASK — исправление CHAT_REACTIONS

ChatGPT, исправь только ошибку, записанную сверху в `AI_HANDOFF.md`.

После migration 12 три существующих auth-теста в `tests/auth/api.test.js` ожидают старую версию базы 11. Поэтому локальный `npm run verify` падает только с `expected 11 / actual 12`.

Нужно:
1. Создать новую отдельную ветку от актуального `codex/local-workspace-snapshot`.
2. Исправить только эти проверки так, чтобы они корректно проверяли текущую версию схемы 12.
3. Не менять production-код реакций, миграцию, карту, рацию, Caddy, пароли или другие блоки.
4. Запустить доступные реальные тесты.
5. Записать в `AI_HANDOFF.md`: имя ветки, commit, список изменённых файлов и результаты тестов.

После этого остановись: Codex проверит fix и продолжит применение.
