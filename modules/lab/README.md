# Lab Module

Status: `active-legacy`.

The live Patap Lab interface currently runs from root files:

```text
index.html
styles.css
app.js
assets/patap-lab-bg.png
```

This module folder is the migration target. Move functionality here incrementally only after each step is verified.
