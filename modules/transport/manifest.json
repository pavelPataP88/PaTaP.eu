{
  "id": "transport",
  "name": "Transport",
  "version": "0.1.0",
  "status": "architecture-only",
  "route": "/transport",
  "futureRoutes": ["/transport", "/truck", "/taxi"],
  "entry": "index.html",
  "roles": ["Administrator", "Developer", "TruckDriver", "TaxiDriver"],
  "permissions": ["transport:open"],
  "storage": ["modules", "projects", "cache", "uploads", "config"],
  "description": "Architecture-only future transport model. Not the current Driver runtime; migration requires an explicit per-domain strangler plan."
}
